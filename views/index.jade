extends layout

block content
  a(href="/new") 
    button.btn.btn-default(type="button", data-dismiss="modal") Add New Message
  h1=title
  - for (var i = 0; i < messages.length; i++)
    li= messages[i].text + ' This is ' + messages[i].user + ' and I was added at ' + messages[i].added
    p= ''